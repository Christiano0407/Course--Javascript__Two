console.group("ADD MUSIC"); 
// === Audio ===
let  wavesurfer = WaveSurfer.create({
    container: '#waveform',
    waveColor: `violet`,
    progressColor: `purple`, 
   // scrollParent: true
});













console.groupEnd; 